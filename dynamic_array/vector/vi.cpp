#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vi;

int main(){
	vi v;                         // created the vector
	v.push_back(20);              // push at the end
	v.insert(v.begin(), 10);      // insert element at the begining of a vector
	v.insert(2, 30);              // insert element at a specific position


}
